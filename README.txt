I look at if an advantage can be gained by handedness of corner outfielders.

load-data.R loads libraries, sets working directory, and loads the data from the data directory.

wrangle-data.R creates a dataset as R object in rda directory.

analysis.R performs regression analysis and provides summary of the regression model.